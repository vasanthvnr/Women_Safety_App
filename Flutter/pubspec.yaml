name: women_safety_app
description: A Flutter app designed for women's safety with UI design and storage functionality.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  firebase_auth: ^4.17.4
  firebase_core: ^2.24.2
  flutter:
    sdk: flutter
  permission_handler: ^11.4.0
  http: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/police_logo.png
